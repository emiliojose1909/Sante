name: sante
description: A new Flutter project.


version: 1.0.0+1

environment:
  sdk: ">=2.1.0 <3.0.0" 

dependencies:
  flutter:
    sdk: flutter
  http: ^0.12.0+1
  provider: ^3.0.0+1
  modal_progress_hud:
  shared_preferences: ^0.5.3+4
  json_model:
  build_runner: ^1.0.0
  json_serializable: ^2.0.0
  flutter_calendar: ^0.0.1
  cupertino_icons: ^0.1.2
  cached_network_image: ^1.1.3

dev_dependencies:
  flutter_test:
    sdk: flutter



flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  assets:
  - assets/images/lock.png
  - assets/images/Logo Sante Odonto@2x.png
  - assets/images/Logo Sante Odonto.png
  - assets/images/Grupo de máscara 3@2x.png
  - assets/images/Grupo de máscara 2@3x.png
  - assets/images/Grupo de máscara 3@3x.png
  - assets/images/Caminho 33@3x.png
  - assets/images/img_958.png
 